// import processing.opengl.*;

void setup() {
  size( 100, 100, OPENGL );
}

void draw() {
  background(0);
  noStroke();
  pointLight(51, 102, 126, 35, 40, 36);
  noStroke();
  translate(80, 50, 0);
  sphere(30);
}