import processing.opengl.*;

void setup(){
  size(100,100,OPENGL);
}

void draw(){
  background(0);
  stroke(255);
  translate(0, height/2, 0);

  for(int i=0; i < 10; i++){
    strokeWeight(i);
    point(i* 10, 0, 0);
  }
}