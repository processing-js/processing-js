import processing.opengl.*;

void setup(){
  size(100,100,OPENGL);
  translate(width/2, height/2,10);
 // noStroke();
  stroke(120);
  beginShape(TRIANGLES);//
  fill(127,0,0);
  vertex(0,-20,0);
  fill(0,127,0);
  vertex(-30,20,0);
  fill(0,0,127);
  vertex(30,20,0);
  endShape();
}