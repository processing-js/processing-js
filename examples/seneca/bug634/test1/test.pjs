import processing.opengl.*;

void setup(){
  size(100,100,OPENGL);
  background(120);
  translate(width/2, height/2,0);

 // noStroke();
  stroke(0);

  beginShape(TRIANGLES);
  fill(127,0,0);
  vertex(0,-20,0);
  fill(0,127,0);
  vertex(-30,20,0);
  fill(0,0,127);
  vertex(30,20,0);
  endShape();
}