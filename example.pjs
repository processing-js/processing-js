// 	Processing.js Example

void setup() {
  size(200, 200);
}

void draw() {
  background(#00ff00);
  if(mouseX < 50) {
    status("left");
  } else {
    status("right");
  }
}
function status(message){
  windows.status = message ;
  text(message,11,22);
}
